<div className="flex flex-wrap gap-6">
  {/* {currentUser || currentUser===null ? ( */}
  {/* Object.keys(cart).length > 0 ? ( */}
  {/* Object.values(cart).map((item) => ( */}
  <div key={item._id} className="flex items-center border-b-2 mb-4 ml-8">
    <img className="w-[100px] h-[100px] bg-center bg-no-repeat rounded-lg" src={item.image} alt="photo" />
    <div className="flex flex-col ml-3">
      <div className="text-sm">
        <span className="text-red-700 text-sm">RAM:</span> {item.storage.RAM} GB
      </div>
      <div className="text-sm">
        <span className="text-red-700 text-sm">ROM:</span> {item.storage.ROM >= 1024 ? `${item.storage.ROM / 1024}TB` :
        `${item.storage.ROM} GB`}
      </div>
      <div className="font-semibold">
        {item.name} - ₹{item.discountedPrice}
      </div>
      <div className="m-2">
        <button className="bg-red-500 text-white rounded-lg p-2 text-sm" onClick={()=>
          handleRemove(item._id)}>Remove</button>
      </div>
    </div>

  </div>
  {/* )) */}
  {/* ) : ( */}
  <p>Cart is empty!</p>
  {/* ) */}
  {/* ) : ( */}
  <p>Sign in to access the cart!</p>
  {/* )} */}
</div>
{Object.keys(cart).length > 0 ?
<div className="mb-5">
  Total: <span className="text-red-700 font-semibold">{totalPrice}</span>
</div> : ' '}